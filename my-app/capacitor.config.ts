import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trafficControl.app',
  appName: 'Traffic Control',
  webDir: 'build'
};

export default config;
